project: sfm-vm-support
host: local

vars:
  vm_ip: ""
  vm_user: ""
  vm_pass: ""

secrets:
  ssh_key: ""

data:
  kubernetes:
    namespace: sfm-1
    deployment: sfm-instance-rest-deployment

  keycloak:
    realm: SFM
    client_id: smart-fabric-manager
    admin_username: keycloak
