apiVersion: cutip/v1
kind: ImageCard
metadata:
  name: web
spec:
  # Use alpine for e2e simplicity — no Dockerfile build required.
  # The scaffold complex project uses source: build with a Python image.
  source: pull
  image: docker.io/library/alpine
  tag: "3.20"
